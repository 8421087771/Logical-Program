package com.app.multithread;

public class DeadLock {
	/*
	 * If two thread are waiting for each other for eversuch type infinite waiting is called
	 * deadlock. 
	 * 
	 * Synchronized keyword is only reason for dead lock situation.
	 * hence while using synch keyword we have to take special care.
	 * there are no resolution tech for deadlock but prevention technics are available.
	 * 
	 * 
	 */}
